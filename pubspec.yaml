name: colorus
description: "Multiple Color Pickers"
version: 0.0.1
homepage: https://www.djarjo.com/colorus

environment:
  sdk: ^3.8.0-67.0.dev
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
